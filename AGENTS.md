# Project workflow

- The user has authorized automatic GitHub uploads after each completed update to this project. After the relevant checks pass, commit the intended changes and push them to `origin` on `main`; do not ask for upload confirmation again for this established workflow.
- The authorized repository is `https://github.com/123456789987654321-art/agent-system.git`. Verify the destination before pushing. Report the resulting commit and whether the remote was updated successfully.
- Stage only files belonging to the requested update. Keep local `output/` previews, temporary artifacts, credentials, and unrelated work out of commits. Do not force-push or discard unrelated changes.
